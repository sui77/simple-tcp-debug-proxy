# simple-tcp-debug-proxy

Shows tcp traffic between a client + server in a browser. I'm using this for XMPP but it will most likely also work fine for any other uncompressed non binary text protocol.

    npm install
    node index.js
    
    # open http://<host>:85
